import { initializeApp } from 'firebase/app';
import { getAuth, signInWithEmailAndPassword } from 'firebase/auth'; // Wait, I don't have an email password. 
// Can't test easily without correct auth.
